name: Viewer tests

on:
  push:
    branches: [main]
    paths:
      - 'site/src/viewer/**'
      - '.github/workflows/viewer-tests.yml'
  pull_request:
    paths:
      - 'site/src/viewer/**'
      - '.github/workflows/viewer-tests.yml'

jobs:
  node-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v6
        with:
          node-version: 24

      # The parse and convert suites check the streaming parser against a real export, which no
      # checkout carries; they run by hand, as the viewer's README says.
      - name: Run the self-contained suites
        run: |
          node site/src/viewer/test/draw-test.mjs
          node site/src/viewer/test/stays-test.mjs
          node site/src/viewer/test/segments-test.mjs
          node site/src/viewer/test/arc-test.mjs
